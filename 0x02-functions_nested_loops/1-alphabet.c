#include <stdio.h>
#include "main.h"
/**
* main - check the code
*
* Return: Always 0.
*/
void print_alphabet(void)
{
	int print_alphabet = i;
	for (int i = 97; i <= 122; i++)
	{
		putchar(i);
	}
return (0);
}
int main()
{
	print_alphabet();
	putchar('\n');
	return (0);
}
